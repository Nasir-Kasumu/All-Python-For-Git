#P4.31
uniqueid = []
sum = 100
seen = set()
uniqueid = str(input("enter a 4 digit PIN"))
	for x in uniqueid:
		if x not in seen:
		uniqueid.append(x)
		seen.add(x)
	buyercount = buyercount + 1
ticketamount = int(input("how many tickets would you like? max 4"))
while ticketamount > 100
sum = sum - ticketamount

print("there are" + sum  + "tickets left")
print("there are" + buyercount + "buyers")